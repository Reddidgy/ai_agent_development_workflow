---
name: context-router
description: A context router that points to other relevant documentation and files to be read on every request.
---

# Router Skill

This skill ensures that the agent reads `context-router.md` in the project root on every request to maintain and expand context.

## Usage
- Trigger: Every user request.
- Action: Read `context-router.md` in the project root and add to the context